import React from "react";

const CurrentBiddingComponent = (id) => {

    return (
        <div>
            <span> The current Highest bid is $ {id}</span>
            <span> The current Highest bid is $ {id}</span>
        </div>

    );
}

export default CurrentBiddingComponent;